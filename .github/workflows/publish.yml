name: Publish to npm

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org

      - name: Upgrade npm for provenance support
        run: npm install -g npm@latest

      - name: Install dependencies
        run: bun install

      - name: Build
        run: bun run build

      - name: Replace workspace:* with real versions
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          echo "Publishing version: $VERSION"
          for pkg in packages/tui/package.json packages/cli/package.json packages/plugin/package.json; do
            sed -i "s|\"workspace:\*\"|\"^${VERSION}\"|g" "$pkg"
            echo "--- $pkg ---"
            cat "$pkg"
          done

      - name: Publish @howlerops/iron-rain
        working-directory: packages/core
        run: npm publish --provenance --access public

      - name: Publish @howlerops/iron-rain-tui
        working-directory: packages/tui
        run: npm publish --provenance --access public

      - name: Publish @howlerops/iron-rain-cli
        working-directory: packages/cli
        run: npm publish --provenance --access public

      - name: Publish @howlerops/iron-rain-plugin
        working-directory: packages/plugin
        run: npm publish --provenance --access public
